# jonatanmichiels.github.io

Personal GitHub Pages site (portfolio/profile).

## Run the dev server

From the project root, start a local Python HTTP server:

```bash
python3 -m http.server 8000
```

Then open http://localhost:8000 in your browser.
